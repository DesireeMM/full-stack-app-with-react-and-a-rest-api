// component to render when resource is not found
const NotFound = () => {
    return (
        <div className="wrap">
            <h2>Not Found</h2>
            <p>Sorry! We can't find what you're looking for.</p>
        </div>
    );
};

export default NotFound;